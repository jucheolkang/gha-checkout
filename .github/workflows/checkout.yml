name : Our Workflow
on : push
jobs : 
  checkout:
    runs-on: ubuntu-latest
    steps:
      - run: ls -al
      - uses: actions/checkout@v3
        with :
          fetch-depth: 3 # 최신 3개를 내려 받는다
      - run : git log --pretty=online
